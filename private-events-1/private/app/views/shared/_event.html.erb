<% events.each do |event| %>
<ul class="container">
    <li>Event Name: <%= link_to event.name, event_path(event) %></li>
    <li>-------------------</li>
    <li>Location: <%= event.location %></li>
    <li><%= time_tag event.date %></li>
</ul>
<% end %>